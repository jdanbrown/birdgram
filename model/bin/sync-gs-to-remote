#!/bin/bash -eux

bin="`dirname "$0"`"
bin="`"$bin"/normpath "$bin"`"
code_dir="`"$bin"/normpath "$bin"/..`"

data_dir="/mnt/disks/disk-bubo-data"
gs_data_dir='gs://bubo-data/v0/data'

# WARNING Careful with -d (delete) -- it'd be easy to make a typo that wipes out all of our cloud data...
# time gsutil -m rsync -r "$@" {"$gs_data_dir","$data_dir"}/hand-labels/
# time gsutil -m rsync -r "$@" {"$gs_data_dir","$data_dir"}/metadata/
# time gsutil -m rsync -r "$@" {"$gs_data_dir","$data_dir"}/cache/22050hz-1ch-16bit/xc
# time gsutil -m rsync -r "$@" {"$gs_data_dir","$data_dir"}/cache/joblib/sp14/model/feat/
# time gsutil -m rsync -r "$@" {"$gs_data_dir","$data_dir"}/cache/joblib/sp14/model/_feat/
# time gsutil -m rsync -r "$@" {"$gs_data_dir","$data_dir"}/xc/metadata/
time gsutil -m rsync -r "$@" {"$gs_data_dir","$data_dir"}/xc/data/ # WARNING Big...
# time gsutil -m rsync -r "$@" {"$gs_data_dir","$data_dir"}/xc/audio-paths.jsonl
